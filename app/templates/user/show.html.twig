{% extends 'base.html.twig' %}

{% block title %}Usuario: {{ user.nombre ?? user.email }}{% endblock %}

{% block body %}
<div class="container-glass">
  <div class="d-flex justify-content-between align-items-center flex-wrap mb-3">
    <h1 class="page-title">{{ user.nombre ?? user.email }}</h1>
    <div class="glass-actions">
      <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="glass-btn primary">
        <i class="bi bi-pencil-square"></i> Editar
      </a>
      <a href="{{ path('app_user_index') }}" class="glass-btn" style="border-color:#B28E5C;color:#B28E5C">
        <i class="bi bi-arrow-left"></i> Regresar
      </a>
    </div>
  </div>

  <div class="glass-card">
    <table class="table table-borderless">
      <tbody>
        <tr><th style="width:150px">ID</th><td>{{ user.id }}</td></tr>
        <tr><th>Nombre</th><td>{{ user.nombre }}</td></tr>
        <tr><th>Email</th><td>{{ user.email ?? '—' }}</td></tr>
      </tbody>
    </table>
  </div>

  {% if is_granted('ROLE_ADMIN') %}
    <div class="mt-3">{{ include('user/_delete_form.html.twig') }}</div>
  {% endif %}
</div>
{% endblock %}
