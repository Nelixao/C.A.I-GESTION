{% extends 'base.html.twig' %}

{% block title %}Nuevo Usuario{% endblock %}

{% block body %}
<div class="container-glass">
  <div class="d-flex justify-content-between align-items-center flex-wrap mb-3">
    <h1 class="page-title">Crear Nuevo Usuario</h1>
    <a href="{{ path('app_user_index') }}" class="glass-btn" style="border-color:#B28E5C;color:#B28E5C">
      <i class="bi bi-arrow-left"></i> Regresar
    </a>
  </div>
  <div class="glass-card">
    {{ form_start(form, { attr: { class: 'form-glass' } }) }}
      {{ form_widget(form) }}
      <div class="form-actions mt-3">
        <button class="glass-btn primary" type="submit">
          <i class="bi bi-save"></i> Guardar
        </button>
      </div>
    {{ form_end(form) }}
  </div>
</div>
{% endblock %}
