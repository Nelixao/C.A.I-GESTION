<form method="post" action="{{ path('app_oficio_delete', {'id': oficio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oficio.id) }}">
    <button class="btn">Delete</button>
</form>
