{% extends 'base.html.twig' %}

{% block title %}Crear Nuevo Oficio{% endblock %}

{% block body %}
<div class="container-glass">

  <!-- Encabezado y acciones -->
  <div class="d-flex justify-content-between align-items-center flex-wrap mb-3">
    <h1 class="page-title">Registrar Nuevo Oficio</h1>

    <div class="glass-actions">
      <a href="{{ path('app_oficio_index') }}" class="glass-btn" style="border-color:#B28E5C;color:#B28E5C">
        <i class="bi bi-arrow-left"></i> Regresar
      </a>
    </div>
  </div>

  <!-- Tarjeta del formulario -->
  <div class="glass-card">
    {{ include('oficio/_form.html.twig', {'button_label': 'Guardar'}) }}
  </div>

</div>
{% endblock %}
