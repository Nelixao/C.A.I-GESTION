<form method="post" action="{{ path('app_correspondence_delete', {'id': correspondence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ correspondence.id) }}">
    <button class="btn">Delete</button>
</form>
