{% extends 'base.html.twig' %}

{% block title %}CISAE — Nuevo Expediente{% endblock %}

{% block body %}
<div class="container-glass">
  <div class="d-flex justify-content-between align-items-center flex-wrap mb-3">
    <h1 class="page-title"><i class="bi bi-plus-circle text-danger me-2"></i>Nuevo Expediente CISAE</h1>
    <div class="glass-actions">
      <a href="{{ path('app_cisae_index') }}" class="glass-btn">
        <i class="bi bi-arrow-left"></i> Volver
      </a>
    </div>
  </div>

  <div class="glass-card">
    {{ include('cisae/_form.html.twig') }}
  </div>
</div>
{% endblock %}
